#include "point.hxx"

/*
float len(sf::Vector2f v) {
    return hypot(v.x, v.y);
}
sf::Vector2f normalize(sf::Vector2f v) {
    float l = len(v);
    if (l == 0) return v;
    return v * (1.0f / l);
}
*/

