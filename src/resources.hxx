#pragma once

struct Resources {
    Resources() :
        money(10000),
        aluminium(0), coal(0), copper(0), diamond(0), gold(0), iron(0)
    { }
    int money, aluminium, coal, copper, diamond, gold, iron, silver;
};

